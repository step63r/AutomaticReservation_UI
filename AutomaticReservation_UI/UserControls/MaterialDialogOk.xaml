<UserControl x:Class="AutomaticReservation_UI.UserControls.MaterialDialogOk"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:AutomaticReservation_UI.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <!-- ViewModelクラス -->
    <UserControl.DataContext>
        <vm:MaterialDialogOkViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="{Binding Path=Message}" FontSize="14" TextWrapping="WrapWithOverflow"/>
        </Grid>
        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="OK" IsDefault="True" Style="{DynamicResource MaterialDesignFlatButton}" Command="{Binding Path=BtnExecute}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
