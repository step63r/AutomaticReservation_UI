<UserControl x:Class="AutomaticReservation_UI.UserControls.MaterialDialogProcessing"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:AutomaticReservation_UI.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <!-- ViewModelクラス -->
    <UserControl.DataContext>
        <vm:MaterialDialogProcessingViewModel/>
    </UserControl.DataContext>
    
    <StackPanel Margin="10">
        <TextBlock Text="{Binding Path=Message}" FontSize="14" HorizontalAlignment="Center"/>
        <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}" HorizontalAlignment="Center" Margin="16" IsIndeterminate="True" Value="0" />
        <!--<Button Content="キャンセル" Style="{StaticResource MaterialDesignFlatButton}" IsCancel="True" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" HorizontalAlignment="Center"/>-->
    </StackPanel>
</UserControl>
